# Prompt Asset Template

This packaged copy keeps the installed `prompt-assets` skill usable after
`skill-installer` copies only this directory.

In the source repository, the canonical source lives at
blueprints/prompts/prompt-asset-template.md.

## Purpose

What task the prompt asset supports.

## Variables

- variable name
- expected type
- intended use

## Constraints

- what the prompt should avoid
- what success looks like
